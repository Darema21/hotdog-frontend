.id-text {
  font-size: 48rpx;
}

.name-text {
  font-size: 64rpx;
}

.mp-swipe-card-item {
  height: 100%;
  width: 100%;
  padding: 20rpx;
  /* background: linear-gradient(to right bottom, red, orange, green, purple); */
}

.mp-swipe-card-container {
  width: 100%;
  height: 100%;
  position: relative;
}

.mp-swipe-card-item {
  width: 100%;
  height: 100%;
  border-radius: 20rpx;
  position: absolute;
  background: #fff;
  overflow: hidden;
}

.top {
  /* background: chartreuse; */
  box-shadow: 1px 1px 1px 1px rgba(0, 0, 0, .1);
}